# Telegram Bot Configuration / Telegram 机器人配置
# Create a bot via @BotFather and get the token / 通过 @BotFather 创建机器人并获取 Token
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Proxy Settings (Optional, for users in restricted regions) / 代理设置（可选，适用于受限地区的用户）
# Host (e.g., 127.0.0.1) / 主机 (例如 127.0.0.1)
# HTTP_PROXY_HOST=127.0.0.1
# Port (e.g., 7890) / 端口 (例如 7890)
# HTTP_PROXY_PORT=7890

# Polymarket / Goldsky Configuration / Polymarket / Goldsky 配置
# API keys or URLs if needed (Managed internally in code for public endpoints currently)
# 如果需要 API 密钥或 URL（目前公共端点在代码内部管理）

# Manual Watchlist (Comma separated addresses) / 手动观察列表（逗号分隔的地址）
# Example: 0x123...,0xabc...
MANUAL_WATCHLIST=

# Filter Settings / 过滤设置
# Max trades per day to be considered a human (Bot detection) / 被视为真人的每日最大交易数（机器人检测）
MAX_DAILY_TRADES=50
# Min Win Rate (0.0 to 1.0) / 最小胜率 (0.0 到 1.0)
MIN_WIN_RATE=0.60

# Copy Trading Settings / 跟单交易设置
# Amount to copy per trade (USDC) / 每笔交易跟单金额 (USDC)
COPY_TRADE_AMOUNT=10.0
# Mode: SIMULATION or REAL / 模式: SIMULATION (模拟) 或 REAL (真实)
TRADE_MODE=SIMULATION
